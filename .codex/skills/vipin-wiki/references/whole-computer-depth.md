# Whole-Computer Depth Policy

Use this when a task touches local project routing, file organization, or whole-computer maintenance.

## Default Rule

Start shallow across `C:/`, `D:/`, and `G:/`. Promote a root to deeper inspection only when live evidence shows active use, high consequence, repeated user interest, or operating-system/agent infrastructure relevance.

Physical file organization is delegated to `D:\agent-resources\skills\vipin\workstation-maintenance`. Use that skill for dry-run manifests, type-grouped age-gated move plans, approved batches, and rollback; use `weiping-wiki` for public-safe routing pages after dry-run evidence or actual state changes. `vipin-wiki` remains a historical alias.

Deep maintenance is not full-source crawling. It means reading enough entry evidence to understand role, importance, boundaries, and cross-project relationships.

## Evidence To Read

For important roots, read only entry evidence first:

- `README*`, `AGENTS.md`, `CLAUDE.md`, `OPENCODE.md`
- `package.json`, `pyproject.toml`, solution/project manifests
- top-level `docs/`, `src/`, `apps/`, `packages/` names
- `git status --short --branch`
- existing wiki pages and catalog search results
- junction/reparse-point targets for renamed roots and historical aliases
- automation prompt/model/reasoning summaries when a root owns scheduled maintenance

Do not deep-read source trees, private files, datasets, logs, browser profiles, DBs, or generated artifacts during routing maintenance.

## Tier Handling

- Tier 0 operating roots get current commands, safety boundaries, validation gates, and links.
- Tier 1 active projects get purpose, path, status, first-read files, related wiki pages, and safety notes.
- Tier 2 archives get content nature, discovery clues, and edit caution.
- Tier 3 bulk/system roots get a bucket line and a reason for low detail.
- Private/sensitive roots get private-only minimal metadata or no public record.

No tier allows broad physical moves under `D:\Research`; resolved paths under that root are excluded from workstation move plans.

## Current High-Importance Agentic Roots

Treat these as at least Tier 0/Tier 1 routing targets during deep maintenance:

| Canonical root | Compatibility alias | Why it matters |
| --- | --- | --- |
| `D:/Research/WEIPING_WIKI` | `D:/Research/vipin's knowledgebase` | Operating contract, public wiki, automations, project maps, and skill rules. |
| `D:/devtools` | `C:/Users/admin/.codex`, `C:/Users/admin/.claude`, and other junction entrypoints | Private runtime, Codex/Claude homes, automations, launchers, health checks. |
| `D:/devtools-public` | none expected | Public-safe infrastructure export. |
| `D:/AGENT_RESOURCE` | `D:/agent-resources` | Shared skill/resource library and routing index. |
| `D:/AGENTIC_SCIENCE` | none expected | Workflow factory source, including UUPF upgrade method. |
| `D:/Research/WEIPING_LAB` | `D:/Research/vipin-lab` | Honest research workbench and ARIS-style workflow runtime. |
| `D:/Research/WEIPING_COUNCIL` | `D:/Research/vipin-council` | Multi-model deliberation/runtime companion to the lab. |

Keep these connected through route pages, skill links, documented optional interfaces, and automation prompts. Do not create hidden runtime coupling between them.

## Promotion Triggers

Promote a root when:

- the user asks about it repeatedly
- it has current README/AGENTS/package evidence
- it appears in recent logs, agentmemory, or active git work
- it affects agent infrastructure, research routing, healthcare/legal/account safety, or public publishing
- it participates in the WEIPING agentic constellation and needs a low-coupling relationship note

Demote or archive a root when live evidence shows it is stale, duplicated, scratch-only, or no longer useful for routing.
